# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/

# Data (raw fetched data — don't commit API results)
data/raw/
data/processed/
# data/snapshot/ is intentionally NOT ignored — it ships a small raw CSV so
# the deployed Streamlit app has data on first load (no live yfinance fetch).

# Outputs (generated — don't commit, let users run it themselves)
outputs/

# Notebooks checkpoints
.ipynb_checkpoints/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Secrets
.env
*.key
